## 2025-02-19 - Removed redundant O(n) scan in inner loop
**Learning:** `np.max([np.max(h.values()) for h in hist_dict.values()])` was being called inside a nested helper function (`hist_dict_fcn`) that executed multiple times for each histogram plotted. Profiling showed this dominated execution time because it was calculating the global max recursively instead of caching it once.
**Action:** Always look for invariants in nested loops and inner functions. Moved the `_max_value_global` calculation outside the `hist_dict_fcn` to speed up plotting. Remember NOT to use `functools.lru_cache` for `hist_dict_fcn` since it returns deepcopies that are mutated by the caller.
## 2026-05-06 - Replaced O(N^2) Uproot path lookups with O(N) directory traversal
**Learning:** Computing metrics like yield and linearity using key-driven, top-down path lookups (e.g., ) is extremely slow for large ROOT files due to redundant string parsing and existence checks. Furthermore, repeatedly calling  inside independent comprehensions duplicates histogram deserialization overhead.
**Action:** Iterate through available Uproot directories first (), retrieve keys dynamically using , fetch objects and call  exactly once per loop, and compute all necessary metrics simultaneously in a single O(N) pass.
## 2025-02-19 - Replaced O(N^2) Uproot path lookups with O(N) directory traversal
**Learning:** Computing metrics like yield and linearity using key-driven, top-down path lookups (e.g., `fitDiag[f"shapes_{fit}/{ch}/{k}"]`) is extremely slow for large ROOT files due to redundant string parsing and existence checks. Furthermore, repeatedly calling `.to_hist()` inside independent comprehensions duplicates histogram deserialization overhead.
**Action:** Iterate through available Uproot directories first (`ch_dir = fitDiag[dir_path]`), retrieve keys dynamically using `ch_dir.keys(cycle=False)`, fetch objects and call `.to_hist()` exactly once per loop, and compute all necessary metrics simultaneously in a single O(N) pass.
