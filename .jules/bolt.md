## 2025-02-19 - Removed redundant O(n) scan in inner loop
**Learning:** `np.max([np.max(h.values()) for h in hist_dict.values()])` was being called inside a nested helper function (`hist_dict_fcn`) that executed multiple times for each histogram plotted. Profiling showed this dominated execution time because it was calculating the global max recursively instead of caching it once.
**Action:** Always look for invariants in nested loops and inner functions. Moved the `_max_value_global` calculation outside the `hist_dict_fcn` to speed up plotting. Remember NOT to use `functools.lru_cache` for `hist_dict_fcn` since it returns deepcopies that are mutated by the caller.
## 2026-04-02 - Optimize nested list operations\n**Learning:** The use of `sum([list1, list2], [])` to flatten lists creates severe O(N^2) bottlenecks due to repeated memory allocations. Using list comprehensions or `itertools.chain` is far more performant. Also, generator expressions like `sum(expr for x in y)` over list comprehensions `sum([expr for x in y])` and `any()` over  avoid temporary full-array creation.\n**Action:** Replaced functional and slow list flattening and generator iterations in `make_plots.py` and `plot_postfits.py` with highly performant standard Python equivalents.
## 2024-05-19 - Optimize nested list operations
**Learning:** The use of `sum([list1, list2], [])` to flatten lists creates severe O(N^2) bottlenecks due to repeated memory allocations. Using list comprehensions or `itertools.chain` is far more performant. Also, generator expressions like `sum(expr for x in y)` over list comprehensions `sum([expr for x in y])` and `any()` over `sum() > 0` avoid temporary full-array creation.
**Action:** Replaced functional and slow list flattening and generator iterations in `make_plots.py` and `plot_postfits.py` with highly performant standard Python equivalents.
